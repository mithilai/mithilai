name: refresh profile readme

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  snake:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: mithilai
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=%23F5A623

      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  fetch-content:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Refresh YouTube videos
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          comment_tag_name: "YOUTUBE"
          feed_list: "https://www.youtube.com/feeds/videos.xml?channel_id=UCdUcDdwM0tZLHnqj1VO2iKQ"
          custom_tags: "videoId/yt:videoId/"
          max_post_count: 3
          template: '<td align="center" width="33%"><a href="$url"><img src="https://img.youtube.com/vi/$videoId/hqdefault.jpg" width="100%"/></a><br/><a href="$url"><b>$title</b></a></td>'
          readme_path: "./README.md"

      - name: Refresh Medium posts
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          comment_tag_name: "BLOG"
          feed_list: "https://medium.com/feed/@mithilmaske"
          max_post_count: 4
          date_format: "mmmm d, yyyy"
          template: '- **[$title]($url)**<br/><sub>$date</sub>$newline'
          readme_path: "./README.md"
